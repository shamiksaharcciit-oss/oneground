# oneground core dependencies, pinned to the versions installed and measured
# in task 001 (Python 3.12.10, Windows, .venv). Every artifact and every
# measurement in this repo was produced with exactly these versions.
#
# Named directly by CLAUDE.md:
numpy==2.5.3
faiss-cpu==1.15.0
pyyaml==6.0.3
zstandard==0.25.0
sentence-transformers==6.0.1
umap-learn==0.5.12
pyarrow==25.0.1
matplotlib==3.11.1
#
# Transitive closure, pinned so the environment is reproducible:
annotated-doc==0.0.5
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
cloudpickle==3.1.2
colorama==0.4.6
filelock==3.32.5
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.30.0
idna==3.19
Jinja2==3.1.6
joblib==1.6.0
llvmlite==0.49.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
narwhals==2.26.0
networkx==3.6.1
numba==0.67.0
packaging==26.3
Pygments==2.21.0
pynndescent==0.6.0
regex==2026.9.3
rich==15.0.0
safetensors==0.8.0
scikit-learn==1.9.0
scipy==1.18.1
shellingham==1.5.4
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.23.2
torch==2.14.0
tqdm==4.70.0
transformers==5.16.1
typer==0.27.2
typing_extensions==4.16.0
#
# Test-runner, added in task 006 for the `live` marker on the one integration
# test that calls the real RunPod API read-only. The unit tests still run
# without it (`python oneground/pod/test_pod.py`).
pytest==9.1.1
iniconfig==2.3.0
pluggy==1.6.0
#
# Qdrant adapter (task 009). The client is the official one, pinned
# like everything else; the rest is its transitive closure.
qdrant-client==1.19.0
grpcio==1.83.1
protobuf==7.36.1
pydantic==2.13.5
pydantic-core==2.46.5
annotated-types==0.8.0
h2==4.4.1
hpack==4.2.0
hyperframe==6.1.0
portalocker==3.2.0
# pywin32 is a Windows-only transitive of qdrant-client (via portalocker).
# Without the marker `pip install -r requirements.txt` fails outright on
# Linux, which is what killed task 011's first pod session after the pod
# was already billing.
pywin32==312; sys_platform == "win32"
typing-inspection==0.4.4
urllib3==2.7.0
#
# Calibration fixture (task 012). The ANN-Benchmarks corpus and its published
# ground truth are distributed as HDF5, and `oneground calibrate fixture` is
# the only thing that reads it -- the fixture's own artifacts are .npy, so
# nothing else in the package imports h5py.
h5py==3.16.0
#
# pgvector adapter (task 015). psycopg 3 with the binary build, so no libpq
# has to be present on the machine. `tzdata` is its Windows transitive.
psycopg==3.3.5
psycopg-binary==3.3.5
tzdata==2026.3
