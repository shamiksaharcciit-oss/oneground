# Two notes for `docs/PRACTICE.md`, and they arrive as a pair

*From tasks 044c and 044d, for the interface stream. Sent together because
they are the same shape from two directions, and the pairing is the argument.*

- **`practice-select-on-provenance-not-locality.md`** — §2, a check that could
  not see its subject. A survey for other instances of a defect returned a
  confident all-clear from a predicate that structurally excluded the case
  that prompted it.

*Neither note writes a warning number in. Both were drafted against a §2 that
ended at warning 7, and warning 8 landed on main while they were being
written — a number in a document you do not own is §1's stale state with extra
steps. The placement and the numbering are yours.*
- **`practice-a-number-on-a-page-has-a-receipt.md`** — a number that could not
  be checked. The lab team's rule, which refused one of our proposals: a
  figure on a page has a receipt in the data the page ships.

## Why together

One is about a **check** with nothing behind it and one is about a **number**
with nothing behind it. In both the artifact looked sound — the survey named
nine candidates with line numbers, the caption stated a figure that was
correct — and in both there was nothing a later reader could hold it against.

That is the same failure twice: **something that reads as evidence and is not
answerable to anything.** A survey whose zero cannot be interrogated, and a
figure whose value cannot be traced. Either one alone reads as a local
mistake; the two together name the property that was missing, which is not
rigour or purity but *answerability after the author has gone*.

They also converge with a rule this project already enforces from the other
side. `docs/STATE.md`'s rendering contract says a view may **tally, never
measure** — it may not compute a number, only report one already in the state.
The lab team's rule says a page may not state a number that is not in its
data. Same constraint on where a figure comes from, reached independently by
two teams from opposite ends, which is about as much evidence as a rule of
this kind ever gets.

## On the page's constraint

Nothing enters the page that has not cost something. **Both have.**

- The survey note cost nine candidates, a false all-clear, and the sentence
  that would have gone into a task report saying no other instance exists —
  resting on a search that could not have found one. Wrong in the direction
  reports are least often corrected in: reassuring.
- The receipt note cost a refused proposal and the task now required to land
  it properly (044e). That price was paid by the lab team rather than by us,
  and the note says so rather than claiming it.

The survey note's rule was also rewritten before sending, at the developer's
instruction, so that **the transferable part leads**: run the audit against
the tree before the fix and require it to name the known instance. That
generalises past the specific mistake to every audit written here, and it is
one run against a commit you already have.

## Two earlier notes, if they are still unplaced

Sent before these and mentioned so the set is visible together:

- `practice-a-key-whose-meaning-differs-by-file.md` (043) — placed as §4.
- `practice-a-rule-written-down-is-not-a-rule-applied.md` (044c) — 043's
  *other* rule, broken by the author of 043's report one task later. Belongs
  beside §4; it is the same task's neighbour.
- `practice-a-synthetic-test-that-implies-a-real-claim.md` (044c) — §2
  warning 8, offered with its own weakness stated. The developer's inclination
  is that it belongs and the reasoning is recorded in the note; the ruling is
  yours.

All of them are yours to place, reword or drop.
