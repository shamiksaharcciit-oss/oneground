# Task 018 — `oneground 0.1.0`

## Expected repo state
`main` after task-017e merged, tree clean, 736 passed / 1 skipped. Work on
`main`; commit `task 018:`; do not push, publish, or tag-and-push — the
developer does those. The release ships **23 September**; nothing in this
task moves that date.

## Why
v0.1 is the first release with a preview week behind it. It must claim
exactly what it does, cite the calibration run that judged it, and install
cleanly on a machine that has never seen this project.

## Do

1. **Carry the two defects from 017.**
   - The winner's-verdict-asserted-of-every-engine class: confirm the
     017f property test covers every generated sentence in the report and
     the HTML, not only `compare_engines`; add any that are uncovered.
   - The workdir-pinning class: audit every test that reads `runs/` and
     assert the rule, not the artifact's current state. List them in the
     report with what each now asserts.

2. **Version and metadata.** `0.1.0` (PEP 440, display `0.1.0`) in
   `oneground/__init__.py` and `pyproject.toml`; description and
   classifiers updated to the shipped scope (two engines, two fixtures);
   extras reviewed — `[qdrant]`, `[pgvector]`, `[pod]`, `[calibrate]`,
   `[view]` — each installing only what it needs, and the guarded pins
   still exact so a bare `pip install oneground` yields the pinned
   environment.

3. **Docs pass — the whole of `docs/`, both READMEs, and the site copy
   under `site/teaser/` that names a version.** Every present-tense claim
   must be true of `0.1.0` on a fresh install. Specifically:
   - `README.md` — what ships today, in the product-page voice, with the
     four commands, the two engines, the two fixtures, and the
     `fixture verify` line an outsider can run.
   - `docs/FIXTURES.md` — already written for both; confirm the numbers
     against the specs by script, as 016 did.
   - `docs/VERIFY.md` — the three-run rule, the same-environment and
     same-configuration rules, the transport/unanswerable distinction,
     the 38.22/42.82 note.
   - `docs/POD.md` — `volume: none`, the baked image by digest, the
     non-reproducible-build property, the phase split.
   - `docs/VALIDATION.md` — the three layers, what is validated by
     reference and what by prediction, the cadence, and the latest
     history line.
   - `docs/INTAKE.md` — the two tiers, the truncation rule ("one record =
     one vector; bring chunks").
   - `docs/CHARTER.md` — status table through 018; roadmap updated:
     more engine adapters (Milvus, Weaviate) behind the `VectorEngine`
     protocol with the conformance suite as the gate, named as the
     community contribution unit; chunking measurement as a v0.2
     characterize feature per `docs/CHUNKING.md`; the lab; proposals.
   - `docs/CHUNKING.md` — new: the chunking ground-truth position
     (four paths, the refusal list, self-retrieval as an upper bound on
     retrievability, the over-fragmentation bias, the home-chunk hit
     definition). I'll supply the text; if it isn't in the repo when you
     reach this step, say so and continue.
   - Anything describing the lab, proposals, chunking, or engines beyond
     Qdrant and pgvector: **planned**, with no date except those on the
     public promise block.

4. **Calibration citation.** Every report cites the latest history line
   for the engine and family it judged, or says none exists. Confirm on a
   fresh run of both fixtures' report paths; if the citation is missing
   or stale, fix. `calibration/history.jsonl` must be current at release.

5. **Release notes** `RELEASE_NOTES.md` rewritten for 0.1.0: what's new
   since the preview in user terms (second engine and matched comparison,
   three-run spread, ceiling, second fixture, truncation warning, faster
   cloud runs), the asset digests, the licence lines for both fixtures,
   the `fixture verify … --asset` instruction, and the known limits
   (sample not corpus; retrieval not generation; no ranking of engines
   without adapters; couldn't-check is an outcome).

6. **Fresh-machine check.** Build sdist + wheel; `twine check`; install
   the wheel into a genuinely fresh venv with no `requirements.txt`;
   run `oneground --version`, `characterize` on a small synthetic `.npy`,
   `fixture verify arxiv-smoke`, and `fixture verify arxiv-150k --asset
   <folder>` against the release asset. Report each command's output.

7. **Tag** `v0.1.0` locally on `main`. Report the commit and tag hashes,
   the wheel and sdist sha256s, the leak scan result, and the suite.

## Acceptance
- Fresh-venv install runs all four commands successfully.
- Leak scan 0; identifier scan clean.
- Docs: no present-tense claim beyond the shipped scope; both fixtures'
  numbers verified against their specs by script.
- Tag exists locally; nothing pushed or published.

## Do not
- Push, publish to PyPI, or create the GitHub release. Change any
  measured value or tolerance. Move the release date.
